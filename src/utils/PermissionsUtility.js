// import { Permissions } from 'expo';

// /**
//  * @name PermissionsUtility.js
//  * @type { function }
//  * @param { String } permissionType
//  * @returns { Boolean }
//  * @description This file contains two utility functions which are used to
//  *              either get existing status or ask user's permission for a 
//  *              particular permission.
//  */

// export const askPermissionAsync = (permissionType) => {

//     return Permissions.askAsync(Permissions[permissionType.toUpperCase()]);
// };

// export const getPermissionAsync = (permissionType) => {

//     return Permissions.getAsync(Permissions[permissionType.toUpperCase()]);
// };